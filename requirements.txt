Flask==3.1.3
ldap3==2.9.1
python-dotenv==1.2.2
cryptography==44.0.2
gunicorn==23.0.0
pyasn1==0.6.1
requests==2.32.3
pyotp==2.9.0
qrcode==8.0
# Optional — only needed for in-app Kerberos/Negotiate SSO (gssapi also requires
# system packages: krb5-devel / libkrb5-dev and a keytab for HTTP/<host>).
# gssapi==1.3.1

